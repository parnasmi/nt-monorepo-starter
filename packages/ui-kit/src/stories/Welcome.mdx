import { Meta } from '@storybook/addon-docs/blocks'

<Meta title='Welcome' />

<div style={{
  fontFamily: 'Inter, -apple-system, BlinkMacSystemFont, sans-serif',
  maxWidth: 900,
  margin: '0 auto',
  padding: '48px 24px',
  color: '#111827'
}}>

{/* ── Hero ─────────────────────────────────────────────────────────── */}

  <div style={{
    background: 'linear-gradient(135deg, #0f172a 0%, #1e293b 60%, #0f172a 100%)',
    borderRadius: 16,
    padding: '48px 40px',
    marginBottom: 48,
    position: 'relative',
    overflow: 'hidden'
  }}>
    <div style={{
      position: 'absolute', top: -60, right: -60,
      width: 240, height: 240,
      background: 'radial-gradient(circle, rgba(99,102,241,0.25) 0%, transparent 70%)',
      borderRadius: '50%'
    }} />
    <div style={{
      position: 'absolute', bottom: -40, left: 80,
      width: 160, height: 160,
      background: 'radial-gradient(circle, rgba(16,185,129,0.18) 0%, transparent 70%)',
      borderRadius: '50%'
    }} />

    <div style={{ position: 'relative' }}>
      <div style={{
        display: 'inline-flex', alignItems: 'center', gap: 8,
        backgroundColor: 'rgba(99,102,241,0.15)',
        border: '1px solid rgba(99,102,241,0.3)',
        borderRadius: 100, padding: '4px 14px',
        fontSize: 12, fontWeight: 500, color: '#a5b4fc',
        marginBottom: 20, letterSpacing: '0.04em', textTransform: 'uppercase'
      }}>
        ✦ Design System
      </div>
      <h1 style={{
        fontSize: 40,
        fontWeight: 800,
        margin: '0 0 16px',
        color: '#f8fafc',
        lineHeight: 1.15,
        letterSpacing: '-0.02em'
      }}>
        Naiton UI Kit
      </h1>
      <p style={{ fontSize: 16, color: '#94a3b8', margin: '0 0 32px', lineHeight: 1.7, maxWidth: 520 }}>
        A production-ready component library for Naiton products — built on Radix UI primitives,
        styled with Tailwind CSS, and fully documented with interactive controls.
      </p>

      <div style={{ display: 'flex', gap: 24, flexWrap: 'wrap' }}>
        {[
          { value: '24', label: 'Components' },
          { value: '100%', label: 'TypeScript' },
          { value: 'ARIA', label: 'Accessible' },
          { value: 'Radix', label: 'Primitives' }
        ].map(({ value, label }) => (
          <div key={label} style={{ textAlign: 'center' }}>
            <div style={{ fontSize: 22, fontWeight: 700, color: '#f8fafc' }}>{value}</div>
            <div style={{ fontSize: 12, color: '#64748b', marginTop: 2 }}>{label}</div>
          </div>
        ))}
      </div>

    </div>

  </div>

{/* ── Feature cards ────────────────────────────────────────────────── */}

<div
	style={{
		display: 'grid',
		gridTemplateColumns: 'repeat(auto-fit, minmax(200px, 1fr))',
		gap: 16,
		marginBottom: 48
	}}
>
	{[
		{
			icon: '🧩',
			title: 'Composable',
			desc: 'Mix and match primitives to build any UI. Every component accepts className and forwards refs.'
		},
		{
			icon: '📖',
			title: 'Autodocs',
			desc: 'Auto-generated prop tables, live controls, and copy-ready code examples for every story.'
		},
		{
			icon: '♿',
			title: 'Accessible',
			desc: 'Built on Radix UI. Follows WAI-ARIA patterns and passes automated a11y checks.'
		},
		{
			icon: '🎨',
			title: 'Themeable',
			desc: 'CSS variables power every token. Swap themes without touching component code.'
		}
	].map(({ icon, title, desc }) => (
		<div
			key={title}
			style={{
				border: '1px solid #E5E7EB',
				borderRadius: 12,
				padding: '24px 20px',
				background: '#fff',
				transition: 'box-shadow 0.2s'
			}}
		>
			<div style={{ fontSize: 26, marginBottom: 12 }}>{icon}</div>
			<h3 style={{ margin: '0 0 8px', fontSize: 15, fontWeight: 600, color: '#111827' }}>{title}</h3>
			<p style={{ margin: 0, fontSize: 13, color: '#6B7280', lineHeight: 1.6 }}>{desc}</p>
		</div>
	))}
</div>

{/* ── UI Components ────────────────────────────────────────────────── */}

  <div style={{ marginBottom: 48 }}>
    <div style={{ display: 'flex', alignItems: 'baseline', gap: 12, marginBottom: 4 }}>
      <h2 style={{ fontSize: 20, fontWeight: 700, margin: 0, color: '#111827' }}>UI Components</h2>
      <span style={{
        fontSize: 12, fontWeight: 600, color: '#6366f1',
        background: '#eef2ff', borderRadius: 100, padding: '2px 10px'
      }}>23</span>
    </div>
    <p style={{ fontSize: 14, color: '#6B7280', margin: '0 0 20px' }}>
      Import from{' '}
      <code style={{ backgroundColor: '#F3F4F6', padding: '2px 6px', borderRadius: 4, fontSize: 13, color: '#374151' }}>
        @/components/ui
      </code>
    </p>

    {[
      {
        category: 'Form Controls',
        color: '#6366f1',
        bg: '#eef2ff',
        items: [
          {
            name: 'Button',
            path: 'UI/Button',
            description: 'Solid, outline, ghost, and link variants with size and state support.'
          },
          {
            name: 'ButtonGroup',
            path: 'UI/ButtonGroup',
            description: 'Groups related buttons with shared border-radius and divider styling.'
          },
          {
            name: 'Checkbox',
            path: 'UI/Checkbox',
            description: 'Toggle between checked, unchecked, and indeterminate states.'
          },
          {
            name: 'Input',
            path: 'UI/Input',
            description: 'Text input with built-in styling, size variants, and error state.'
          },
          {
            name: 'InputGroup',
            path: 'UI/InputGroup',
            description: 'Compose inputs with leading/trailing addons, icons, and buttons.'
          },
          {
            name: 'Label',
            path: 'UI/Label',
            description: 'Accessible label that inherits disabled styling from its associated control.'
          },
          {
            name: 'RadioGroup',
            path: 'UI/RadioGroup',
            description: 'A set of radio buttons — at most one can be selected at a time.'
          },
          {
            name: 'Select',
            path: 'UI/Select',
            description: 'Styled select dropdown with keyboard navigation and option groups.'
          },
          {
            name: 'Textarea',
            path: 'UI/Textarea',
            description: 'Auto-resizable multiline text input with consistent form styling.'
          },
        ]
      },
      {
        category: 'Layout & Structure',
        color: '#10b981',
        bg: '#ecfdf5',
        items: [
          {
            name: 'Field',
            path: 'UI/Field',
            description: 'Composes label, control, and description into an accessible form field.'
          },
          {
            name: 'Separator',
            path: 'UI/Separator',
            description: 'Horizontal or vertical visual/semantic divider between content sections.'
          },
          {
            name: 'Tabs',
            path: 'UI/Tabs',
            description: 'Tabbed navigation that switches between layered sections of content.'
          },
        ]
      },
      {
        category: 'Overlays & Menus',
        color: '#f59e0b',
        bg: '#fffbeb',
        items: [
          {
            name: 'ContextMenu',
            path: 'UI/ContextMenu',
            description: 'Right-click triggered menu with support for submenus and separators.'
          },
          {
            name: 'Dialog',
            path: 'UI/Dialog',
            description: 'Modal window that traps focus and renders primary content inert.'
          },
          {
            name: 'DropdownMenu',
            path: 'UI/DropdownMenu',
            description: 'Trigger-anchored menu for actions, links, and selection items.'
          },
          {
            name: 'Sheet',
            path: 'UI/Sheet',
            description: 'Slide-in panel from any edge — a lighter alternative to a full modal.'
          },
          {
            name: 'Sonner',
            path: 'UI/Sonner',
            description: 'Toast notifications with promise, success, error, and info variants.'
          },
          {
            name: 'Tooltip',
            path: 'UI/Tooltip',
            description: 'Lightweight popover that reveals context on hover or focus.'
          },
        ]
      },
      {
        category: 'Data Display',
        color: '#ec4899',
        bg: '#fdf2f8',
        items: [
          {
            name: 'Avatar',
            path: 'UI/Avatar',
            description: 'Image or initials fallback for user and entity representations.'
          },
          {
            name: 'Badge',
            path: 'UI/Badge',
            description: 'Compact status, category, or count indicator with semantic color variants.'
          },
          {
            name: 'Slider',
            path: 'UI/Slider',
            description: 'Range input with single or multiple thumbs and step support.'
          },
          {
            name: 'Spinner',
            path: 'UI/Spinner',
            description: 'Animated loading indicator in multiple sizes and color variants.'
          },
          {
            name: 'Table',
            path: 'UI/Table',
            description: 'Semantic HTML table with sortable columns, striped rows, and sticky headers.'
          },
        ]
      }
    ].map(({ category, color, bg, items }) => (
      <div key={category} style={{ marginBottom: 28 }}>
        <div style={{
          display: 'inline-flex', alignItems: 'center', gap: 6,
          backgroundColor: bg,
          border: `1px solid ${color}33`,
          borderRadius: 100,
          padding: '3px 12px',
          fontSize: 12, fontWeight: 600, color,
          marginBottom: 10
        }}>
          {category}
        </div>
        <div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
          {items.map(({ name, path, description }) => (
            <div key={name} style={{
              display: 'flex',
              alignItems: 'center',
              gap: 12,
              padding: '10px 14px',
              border: '1px solid #E5E7EB',
              borderRadius: 8,
              backgroundColor: '#FAFAFA'
            }}>
              <code style={{
                fontSize: 12, fontWeight: 600, color: '#111827',
                backgroundColor: '#E5E7EB',
                padding: '2px 8px', borderRadius: 4, whiteSpace: 'nowrap',
                minWidth: 'fit-content'
              }}>
                {name}
              </code>
              <span style={{ fontSize: 13, color: '#6B7280', lineHeight: 1.5 }}>{description}</span>
              <span style={{
                marginLeft: 'auto',
                fontSize: 11,
                color: '#9CA3AF',
                whiteSpace: 'nowrap',
                fontFamily: 'monospace'
              }}>
                {path}
              </span>
            </div>
          ))}
        </div>
      </div>
    ))}

  </div>

{/* ── Shared Components ────────────────────────────────────────────── */}

<div style={{ marginBottom: 48 }}>
	<div style={{ display: 'flex', alignItems: 'baseline', gap: 12, marginBottom: 4 }}>
		<h2 style={{ fontSize: 20, fontWeight: 700, margin: 0, color: '#111827' }}>Shared Components</h2>
		<span
			style={{
				fontSize: 12,
				fontWeight: 600,
				color: '#10b981',
				background: '#ecfdf5',
				borderRadius: 100,
				padding: '2px 10px'
			}}
		>
			1
		</span>
	</div>
	<p style={{ fontSize: 14, color: '#6B7280', margin: '0 0 20px' }}>
		Import from{' '}
		<code style={{ backgroundColor: '#F3F4F6', padding: '2px 6px', borderRadius: 4, fontSize: 13, color: '#374151' }}>
			@/components/shared
		</code>
	</p>
	<div style={{ display: 'flex', flexDirection: 'column', gap: 6 }}>
		{[
			{
				name: 'SvgIcon',
				path: 'Shared/SvgIcon',
				description: 'Clones an SVG element and injects width, height, and className props.'
			}
		].map(({ name, path, description }) => (
			<div
				key={name}
				style={{
					display: 'flex',
					alignItems: 'center',
					gap: 12,
					padding: '10px 14px',
					border: '1px solid #E5E7EB',
					borderRadius: 8,
					backgroundColor: '#FAFAFA'
				}}
			>
				<code
					style={{
						fontSize: 12,
						fontWeight: 600,
						color: '#111827',
						backgroundColor: '#E5E7EB',
						padding: '2px 8px',
						borderRadius: 4,
						whiteSpace: 'nowrap'
					}}
				>
					{name}
				</code>
				<span style={{ fontSize: 13, color: '#6B7280' }}>{description}</span>
				<span
					style={{
						marginLeft: 'auto',
						fontSize: 11,
						color: '#9CA3AF',
						whiteSpace: 'nowrap',
						fontFamily: 'monospace'
					}}
				>
					{path}
				</span>
			</div>
		))}
	</div>
</div>

{/* ── Getting Started ──────────────────────────────────────────────── */}

  <div style={{ borderTop: '1px solid #E5E7EB', paddingTop: 40 }}>
    <h2 style={{ fontSize: 20, fontWeight: 700, margin: '0 0 8px', color: '#111827' }}>Getting Started</h2>
    <p style={{ fontSize: 14, color: '#6B7280', margin: '0 0 20px', lineHeight: 1.7 }}>
      Import components from{' '}
      <code style={{
        backgroundColor: '#F3F4F6',
        padding: '2px 6px',
        borderRadius: 4,
        fontSize: 13,
        color: '#374151'
      }}>@/components/ui</code>.
      {' '}Wrap form controls in a{' '}
      <code style={{
        backgroundColor: '#F3F4F6',
        padding: '2px 6px',
        borderRadius: 4,
        fontSize: 13,
        color: '#374151'
      }}>Field</code>
      {' '}for accessible labelling and descriptions.
    </p>
    <pre style={{
      backgroundColor: '#0f172a',
      border: '1px solid #1e293b',
      borderRadius: 10,
      padding: '24px 20px',
      fontSize: 13, lineHeight: 1.8,
      overflowX: 'auto', margin: 0,
      color: '#e2e8f0'
    }}>
      {`import { Button }                              from '@/components/ui/button'
      import { Field, FieldDescription, FieldLabel } from '@/components/ui/field'
      import { Input }                               from '@/components/ui/input'

      export function LoginForm() {
        return (
          <form className="flex flex-col gap-4">
            <Field>
              <FieldLabel htmlFor="email">Email</FieldLabel>
              <Input id="email" type="email" placeholder="you@example.com" />
            </Field>
            <Field>
              <FieldLabel htmlFor="password">Password</FieldLabel>
              <Input id="password" type="password" placeholder="••••••••" />
              <FieldDescription>Must be at least 8 characters.</FieldDescription>
            </Field>
            <Button variant="primary">Sign in</Button>
          </form>
        )
      }`}

  </pre>
  </div>

</div>
